import styled from "@emotion/styled";

const Modal = () => {
  return (
    <Container>
      <div>Header</div>
      <div>Content</div>
      <div>Footer</div>
    </Container>
  );
};

const Container = styled.div({});

export default Modal;
