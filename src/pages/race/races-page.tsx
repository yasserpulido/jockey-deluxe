import { Detail } from "./components/detail";
import { Provider } from "./providers";

const RacesPage = () => {
  return (
    <Provider>
      <Detail />
    </Provider>
  );
};

export default RacesPage;